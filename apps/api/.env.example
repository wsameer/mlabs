# API Environment Variables (Standalone Mode)
# NOTE: When running via monorepo (pnpm dev), use root .env instead!
# This file is only needed if you run the API app independently.

# Environment
NODE_ENV=production

# Server
PORT=3000

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/mlabs_dev

# Logging
LOG_LEVEL=info

# CORS - comma-separated origins
CORS_ORIGIN=http://localhost:5173,http://localhost:5174
